<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Express-Lab</title>
</head>
<body>
    <h1>Space Movies</h1>
    <ul>
        <% movies.forEach(function(movie) { %>
            <li>
                <%= movie.movie %>
                -
                <%= movie.good ? 'good' : 'not so good' %>
            </li>
        <% }); %>
    </ul>
</body>
</html>